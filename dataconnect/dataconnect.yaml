specVersion: "v1beta"
serviceId: "vetwebapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vetwebapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
